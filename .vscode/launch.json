{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Run Fungorium",
      "request": "launch",
      "mainClass": "Main",
      "cwd": "${workspaceFolder}",
      "classPaths": [
        "${workspaceFolder}/bin",
        "${workspaceFolder}/src",
        "${workspaceFolder}/Images"
      ]
    }
  ]
}
